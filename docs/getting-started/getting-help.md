---
sidebar_position: 4
title: Getting Help
---

# Getting Help

> **Placeholder content.** Outline for review; verify against sources.

Will cover:

- Making envy talk: `--verbose` (the "why did it do that" narrative), `-q`
  (warnings and errors only).
- `--trace` for machine-readable JSONL event streams — what to attach to a bug
  report. Pointer to [Logging & Tracing](/reference/observability).
- Reading envy output: human messages go to stderr; stdout is reserved for
  machine-readable results (`envy product`, `envy package`, `envy hash`,
  `envy export`).
- Common first-run problems and where the answers live
  ([Troubleshooting](/reference/troubleshooting)).
- Where to file issues; how to report reproducibly (envy version, manifest,
  trace file).
